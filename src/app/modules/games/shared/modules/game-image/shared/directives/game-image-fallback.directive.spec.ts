import { GameImageFallbackDirective } from './game-image-fallback.directive';

describe('GameImageFallbackDirective', () => {
  it('should create an instance', () => {
    const directive = new GameImageFallbackDirective();
    expect(directive).toBeTruthy();
  });
});
